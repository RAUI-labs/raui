[package]
name = "guide_01"
version = "0.1.0"
authors = ["RAUI Contributors"]
edition = "2024"

publish = false

[dependencies]
# The main RAUI crate
raui = { version = "0.70", path = "../../../", features = [
    "app",
    "import-all",
] }
