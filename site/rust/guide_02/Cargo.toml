[package]
name = "guide_02"
version = "0.1.0"
authors = ["RAUI Contributors"]
edition = "2021"

publish = false

[dependencies]
# The main RAUI crate
raui = { version = "0.64", path = "../../../", features = ["app"] }
