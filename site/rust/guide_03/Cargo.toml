[package]
name = "guide_03"
version = "0.1.0"
authors = ["RAUI Contributors"]
edition = "2021"

publish = false

[dependencies]
# The main RAUI crate
raui = { version = "0.42", path = "../../../" }
# The RAUI quickstart crate will help us get our UI rendering as easy as possible
raui-quick-start = { version = "0.42", path = "../../../raui-quick-start" }